/**
 * Format the array or stringed array to string
 * by adding the specified delimited_char
 * after every comma and opening bracket
 *
 * @param arr_str
 */
export declare function array(arr: string | [], delimiter_char?: string): string;
