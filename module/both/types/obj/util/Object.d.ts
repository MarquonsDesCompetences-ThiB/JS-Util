/**
 * Return all values in obj having the specified keys
 *
 * @param obj
 * @param keys
 * @returns
 */
export declare function values(obj: any, keys: string[]): string[];
/**
 * Return all couples [key, value] in obj
 * with every key being in the specified keys array
 *
 * @param obj
 * @param keys
 * @returns
 */
export declare function entries(obj: any, keys: string[]): [string, any][];
